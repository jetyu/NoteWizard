## 1. 编辑器事件与建议状态

- [x] 1.1 为编辑器文档变化增加来源分类和外部同步标记，并让选择变化能够通知自动续写编排。
- [x] 1.2 将行内建议状态扩展为剩余文本与首次提示，支持整段、逐词、逐句接受及相应的接受类型标记。
- [x] 1.3 更新简体中文快捷键提示和建议样式，确保提示只在当前编辑器会话首次显示。

## 2. 上下文与结果质量

- [x] 2.1 增加 Renderer 自动续写上下文构建，提取标题、当前章节、2,000 字符前文、500 字符后文和位置意图。
- [x] 2.2 增加确定性结果清理与重复检测，覆盖前文重叠、后文重叠、空结果和近似重复。
- [x] 2.3 扩展共享、Bridge、Preload 和 Main 的补全载荷校验，保持只有 `context` 的旧载荷兼容。
- [x] 2.4 更新中英文自动续写提示词，按位置意图控制长度并明确全局偏好的兼容优先级。

## 3. 自动续写编排

- [x] 3.1 重构自动续写请求序列和快照校验，使内容、选择、光标或笔记变化后旧结果无法显示。
- [x] 3.2 让普通键入在旧请求期间重新防抖，并让粘贴、历史、格式化、外部同步和部分接受只失效而不触发。
- [x] 3.3 将完整接受后的固定延迟替换为触发策略相关的自适应观察期，并在用户继续输入时切换到普通输入时序。
- [x] 3.4 在 `EditorPane` 传递笔记身份、标题、变化来源、选择活动和本地化首次提示。

## 4. 回归验证

- [x] 4.1 增加上下文意图、原文去重和后文衔接清理的单元测试。
- [x] 4.2 增加在途输入、光标/笔记变化、来源过滤和自适应连续预测的编排测试。
- [x] 4.3 增加逐词、逐句、整段接受与首次提示的编辑器扩展测试。
- [x] 4.4 运行 OpenSpec 严格校验、Main 与 Preload 构建、Renderer 类型检查、相关单元测试和改动文件 lint。
