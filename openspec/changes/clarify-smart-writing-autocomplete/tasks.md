## 1. 自动续写行为

- [x] 1.1 在自动补全 composable 中同时校验智能写作总开关与 `autoContinue`，确保关闭自动续写后普通输入和接受建议都不会安排预测。
- [x] 1.2 更新编辑器配置监听，使关闭智能写作或自动续写时立即清除计时器、在途请求和行内建议，同时保持右键写作操作只依赖总开关。

## 2. 设置页信息架构

- [x] 2.1 将智能写作设置卡片重排到“基础设置”“写作偏好”“自动续写”“快捷操作”四个区域，并添加与现有设置页一致的紧凑区域样式。
- [x] 2.2 让触发策略仅在智能写作与自动续写同时开启时可编辑；语调风格、应用场景和快速翻译只跟随智能写作总开关。
- [x] 2.3 更新简体中文区域标题、自动续写、触发策略与全局写作偏好文案，使设置作用域明确。

## 3. 全局写作偏好

- [x] 3.1 提取可供自动续写和编辑器任务共同复用的中英文语调风格与应用场景提示词片段。
- [x] 3.2 在 Main 端解析右键智能写作请求时，将当前全局写作偏好加入各编辑器 preset 提示词，同时保持任务和翻译约束优先。

## 4. 回归验证

- [x] 4.1 增加自动续写回归测试，覆盖开启时安排预测、关闭时不安排预测，以及关闭后清理待处理工作的行为。
- [x] 4.2 验证自动续写关闭时右键智能写作菜单仍可用，智能写作关闭时相关菜单不可用。
- [x] 4.3 增加提示词测试，覆盖自动续写与右键操作共同使用全局偏好，以及翻译约束保持优先。
- [x] 4.4 运行 `npm run build:main`、`npm run test:unit`、`npm run typecheck` 和 `npm run lint`，并修复本变更引入的问题。
