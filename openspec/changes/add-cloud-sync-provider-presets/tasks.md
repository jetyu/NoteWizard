## 1. Preset foundation

- [x] 1.1 Add typed WebDAV and S3 service preset definitions, detection, and safe application helpers.
- [x] 1.2 Reuse the AI provider dropdown structure and shared styling without adding a separate component.

## 2. Configuration UI

- [x] 2.1 Integrate WebDAV provider selection, common placeholders, and official guide link.
- [x] 2.2 Integrate object-storage provider selection, recommended Endpoint, required Region input, and official guide link.
- [x] 2.3 Add Simplified Chinese labels and hints for all new provider setup UI.
- [x] 2.4 Add local provider logos to both dropdown triggers and options.
- [x] 2.5 Stabilize provider trigger height and mark all connection-required fields.

## 3. Security and compatibility

- [x] 3.1 Allow only the selected providers' official HTTPS documentation hosts in external navigation.
- [x] 3.2 Add unit coverage for preset detection, addressing style, and preservation of sensitive configuration.

## 4. Verification

- [x] 4.1 Run targeted unit tests, renderer type checking, lint, main-process build, locale validation, and OpenSpec validation.
- [x] 4.2 Re-run targeted tests, type checking, lint, renderer build, and strict OpenSpec validation after the presentation updates.
